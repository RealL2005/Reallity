# Reallity

## 有效代码行数统计

统计范围：`src/`、`tests/`、`bin/` 下的 TypeScript/TSX/JavaScript 源码文件（共 53 个文件），已排除 `node_modules`、`.git`、`docs/`、`trace.html`、`.reallity/.superpowers` 以及 Markdown/JSON/HTML 等非代码文件。

统计方法：逐文件去除块注释（`/* ... */`）、行注释（`// ...`）及空行后，统计剩余有效代码行（统计时对字符串/模板字符串内的 `//` 做了识别，避免误删）。

| 目录 | 文件数 | 总行数 | 有效代码行数 |
|------|-------|--------|-------------|
| `src/` | 28 | 5523 | 5041 |
| `tests/` | 24 | 2778 | 2404 |
| `bin/` | 1 | 32 | 27 |
| **合计** | **53** | **8333** | **7472** |

> 原始总行数（含空行与注释）为 8333 行，去除空行和注释后有效代码为 7472 行。
